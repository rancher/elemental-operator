apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: '{{ .Release.Name }}'
rules:
- apiGroups:
  - elemental.cattle.io
  resources:
  - machineinventories
  verbs:
  - get
  - list
  - patch
  - update
